{
  "name": "derbawka-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "posttest": "rm -rf .playwright-mcp && rm -f ./*.png"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "serve": "^14.2.3"
  }
}
